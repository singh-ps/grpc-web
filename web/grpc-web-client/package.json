{
  "name": "grpc-web-client",
  "version": "0.1.0",
  "dependencies": {
    "http-server": "14.0.0"
  },
  "scripts": {
    "start": "npm run build && http-server . -p 8080",
    "build": "tsc && tsc --build",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
